<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css" />
        <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
        <title><%= locals.headTitle.pageName %><%= locals.headTitle.remainder %></title>
    </head>
    <body class="container mb-5 pb-5">
        <div class="this-will-be-hero my-5 d-flex flex-column justify-content-center align-items-center">
            <h1><a href="/">NextTrack</a></h1>
            <h2>Music recommendations in your control</h2>
        </div>

        <form method="POST" action="/recommendations" class="my-5">
            <div class="form-group">
                <label for="spotify_trackLink" class="mb-2">Submit your Spotify track link:</label>
                <input name="spotify_trackLink" id="spotify_trackLink" type="text" class="form-control mb-2" placeholder="https://open.spotify.com/track/..." autocomplete="off" required />
                <button type="submit" class="btn btn-primary">Submit</button>
                <button type="button" class="btn btn-warning" onclick="clearInput()">Clear</button>
            </div>
        </form>

        <div class="this-will-be-examples my-5">
            <h3>Examples</h3>
            <div class="row">
                <% exampleTracks.forEach((exampleTrack, index) => { %>
                <div class="col-md-4">
                    <div class="m-2">
                        <div class="mb-2">
                            <span><%= exampleTrack.artistAndName %></span>
                            <br />
                            <i class="text-muted"> (<%= exampleTrack.note %>)</i>
                        </div>
                        <div class="d-flex">
                            <button type="button" class="btn btn-outline-secondary btn-sm me-2" onclick="copyToClipboard(this)">
                                <i class="bi bi-clipboard">Copy</i>
                            </button>
                            <code class="user-select-all"><%= exampleTrack.link %></code>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>

        <div class="this-will-be-guide my-5">
            <h3>Here's how you can get a link</h3>
            <ol>
                <li>Hover over your desired song on Spotify</li>
                <li>Right click</li>
                <li>Select "Share"</li>
                <li>Select "Copy link to Song"</li>
            </ol>
            <p>
                <i>show gif of how it is done on both <u>desktop</u> and <u>mobile</u></i>
            </p>
        </div>

        <script>
            function clearInput() {
                document.getElementById("spotify_trackLink").value = "";
            }
            async function copyToClipboard(button) {
                let element = button.parentElement.querySelector(".user-select-all");
                await navigator.clipboard.writeText(element.textContent);
                // Some feedback (i.e. icon flashes) upon successful copy
                let icon = button.querySelector("i");
                icon.classList.remove("bi-clipboard");
                icon.classList.add("bi-clipboard-check-fill");
                // Change back after 1 second
                setTimeout(() => {
                    icon.classList.remove("bi-clipboard-check-fill");
                    icon.classList.add("bi-clipboard");
                }, 1000);
            }
        </script>
    </body>
</html>
